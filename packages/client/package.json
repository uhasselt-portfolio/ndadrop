{
  "private": true,
  "name": "client",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "dependencies": {
    "preact": "^10.10.0",
    "preact-render-to-string": "^5.2.1",
    "preact-router": "^3.2.1",
    "socket.io-client": "^4.5.2"
  },
  "devDependencies": {
    "preact-cli": "^3.4.0",
    "sirv-cli": "^2.0.2",
    "typescript": "^4.5.2"
  }
}
