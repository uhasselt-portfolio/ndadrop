{
  "private": true,
  "name": "client",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender --dest ../server/public",
    "serve": "sirv build --cors --single --p 8005",
    "dev": "preact watch --p 8005",
    "dev-ui": "npx tailwindcss -i src/style/tailwind.css -c tailwind.config.js -o src/style/index.css --watch",
    "lint": "eslint src"
  },
  "dependencies": {
    "preact": "^10.10.0",
    "preact-render-to-string": "^5.2.1",
    "preact-router": "^3.2.1",
    "react-router-dom": "^6.4.1",
    "socket.io-client": "^4.5.2",
    "tailwindcss": "^3.1.8"
  },
  "devDependencies": {
    "preact-cli": "^3.4.0",
    "sirv-cli": "^2.0.2",
    "typescript": "^4.5.2"
  }
}
